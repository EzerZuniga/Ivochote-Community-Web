---
import { SITE } from '../../config/site.config';
---
<footer class="bg-gray-50 border-t mt-8">
  <div class="max-w-5xl mx-auto p-6 text-sm text-gray-600">
    <div>{SITE.title} — {SITE.description}</div>
    <div class="mt-2">© {new Date().getFullYear()} {SITE.title}</div>
  </div>
</footer>
